import React from "react";
import { withRouter } from "react-router-dom";
import "./style.scss";
const Head = () => {
  // let { history, location, isMobile } = props;
  return (
    <>
      <header></header>
    </>
  );
};

export default withRouter(Head);
